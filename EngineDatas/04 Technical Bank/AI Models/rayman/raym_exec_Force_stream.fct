int			ti_i	
object	to_stream 
vector	tv_stream 

for ( ti_i = 0; ti_i < @get_list_manager i_stream_nb; ti_i++)
{
	to_stream = @get_list_manager ao_stream[ti_i]
	v_force_STREAM += @to_stream PROC_StreamGet( OBJ_PosGet())
}
