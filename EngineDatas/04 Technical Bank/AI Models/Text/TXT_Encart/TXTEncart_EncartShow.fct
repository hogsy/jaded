object 	T
int			i_effect

T = @"univ" Texte_InGame
if ( !T )
{
	STR_CreateText( mtxt_Encart, mv_EncartPos, 0 )
	return
}

ENCART_HideDialog( T )
if (mi_Text_Displayed)
	return
	
mi_Text_Displayed = 1
ENCART_Show( T, mtxt_Encart, cvector( 0.5, 0.965, 0), 9 )
	

