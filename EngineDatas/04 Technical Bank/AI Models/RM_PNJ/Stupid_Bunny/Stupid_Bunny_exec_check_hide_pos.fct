
float		tf_norm
object	to_wp
vector	tv_dest_pos
vector	tv_ray_dir

tv_dest_pos = pop
to_wp = pop

push(vrai)
return

tv_ray_dir = @o_main_actor OBJ_PosGet()
tv_ray_dir.z += 1.0
tv_ray_dir -= tv_dest_pos

tf_norm = MATH_VecNorm(tv_ray_dir)
tv_ray_dir /= tf_norm

if (COL_RayIsCut_Dist(tv_dest_pos, tv_ray_dir, tf_norm, all, OBJ_C_IdentityFlag_Anims, Ci_Filter_IdentityFlag, COL_C_Ray_on_ColMap_NoCrossable))
	push(vrai)
else
	push(faux)


