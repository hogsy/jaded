#include "PNJ_Lapin_defines.var"

vector	tv_sight


if( i_way_dont_move_dest_in_occluder)
	tv_sight = v_next_pos - OBJ_PosGet()
else if( ! MATH_VecNullToler(v_force_sight, 0.01) )
	tv_sight = v_force_sight
else
	tv_sight = v_joy_sight


OBJ_BankingGeneralSet(MATH_VecBlendRotate(OBJ_SightGet(), tv_sight, f_orient_speed * TIME_GetDt()), v_ground_normale)
f_orient_speed = 5.0

