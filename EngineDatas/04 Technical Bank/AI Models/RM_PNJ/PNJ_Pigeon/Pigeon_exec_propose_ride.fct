
messageid				tmid_mid
object					to_serveur
vector					tv_serveur_pos
float						tf_dist


tmid_mid = pop

to_serveur = MSG_GlobalGetGao(tmid_mid, SERVEUR)
tv_serveur_pos = @to_serveur OBJ_PosGet()

// TEST SI RM DS VOLUME
if ( PROC_Valid_Ride(C_ID_Scooter, tv_serveur_pos, to_serveur))
{
	tf_dist = MATH_VecDistance( tv_serveur_pos, OBJ_PosGet())
	tf_dist = MATH_FloatMin(tf_dist, 999)
	LNK_ClientPropose(tmid_mid, tf_dist)
	LNK_GrabObjectIdSet(tmid_mid, C_ID_Pigeon)
	i_bored_by_someone = faux
}
