if ( mi_DieWhenNoEnnemyLeft && !OBJ_CapaTest( OBJ_Capa_15) )
{
	// nombre d'ennemi encore actif
	AI_Execute( "Spawner_countgen" )
	if (mi_EnemyActive)
		return
}

OBJ_CapaSet( OBJ_Capa_1 | OBJ_Capa_15 , 0 )
if ( AI_TriggerIsValid( mt_EndTrigger) )
	call_trigger( mt_EndTrigger )
	
if ( SF_i_NePasRejouerSiMort )
	Super_SpecialFlag_set( SF_i_AlreadyDead)

AI_TrackCurStop()