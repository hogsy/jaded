
// Test Event Stuct
if( ! OBJ_FlagsIdentityTest(OBJ_C_IdentityFlag_Events) )
{
	DBG_Error("Ce GAO a l'IA ActivatorEvent mais n'a pas d'event....")
	OBJ_Destroy()
}

// Cpt Conditions
i_Trig_Count = 0
if (AI_TriggerIsValid(Trig_Condition1))
{
	i_Trig_Count++
 	if ( AI_TriggerIsValid(Trig_Condition2))
 	{
		i_Trig_Count++
 		if ( AI_TriggerIsValid(Trig_Condition3))
 			i_Trig_Count++
	}
}

AI_TrackChange(1, "AE_WaitDestroy")
AI_TrackCurChangeNow("AE_WaitActivation")
