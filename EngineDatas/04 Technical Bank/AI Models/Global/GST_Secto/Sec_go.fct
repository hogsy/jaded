int		ti_loop

for (ti_loop = 0; ti_loop < 50; ti_loop++)
{
	if (!SCT_Gao[ti_loop])
		break
	
	if (@get_rayman COL_Pivot_BVCollide(SCT_Gao[ti_loop]))
	{
		if ( SCT_Status[ti_loop] != vrai)
		{
			SCT_Status[ti_loop] = vrai
			SCT_Add(SCT_ID[ti_loop], 0x00010000)
		}
	}
	else
	{
		if ( SCT_Status[ti_loop] != faux)
		{
			SCT_Status[ti_loop] = faux
			SCT_Remove(SCT_ID[ti_loop], 0x00010000)
		}
	}
}