#include "RM_Ascenseur_defines.var"

// Réactiver les sons
if (i_etat_courant == ETAT_RMA_MOVE)
	i_SND_move_loop = SND_RequestPlayLoop(SND_MOVE_LOOP)
else if (i_etat_courant == ETAT_RMA_WAIT)
	i_SND_wait_loop = SND_RequestPlayLoop(SND_WAIT_LOOP)
