#include "Ambiance_defines.var"

#ifndef _FINAL_
if( ! i_DEBUG )
{
	if( ! OBJ_FlagsIdentityTest(OBJ_C_IdentityFlag_Sound) )
	{
		DBG_Error("THIS IA MUST HAVE A SOUND BANK")
	}
}
#endif

//i_Instance_Transition = SND_Request(Ci_Transition_Rank, 0)
//i_Instance_Transition1 = SND_Request(Ci_Transition_Rank+1, 0)

AI_TrackCurChange("Ambiance_Loop")		// pas de now car Rayman pas encore téléporté, donc attendre qu'il soit à la position voulue (sinon test area fucked)
