if( AI_TriggerIsValid(t_Go) )
{
	if( call_trigger(t_Go) )
	{
		if( ! o_SnkOwner)
			o_SnkOwner = OBJ_Me()
			
		@o_SnkOwner SND_Prefetch(i_SnkRank, f_Life)
		
		OBJ_Destroy()
	}
}
else
{
	OBJ_Destroy()
}