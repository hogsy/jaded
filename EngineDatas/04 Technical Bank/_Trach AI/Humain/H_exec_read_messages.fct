message tm_msg

tm_msg = MSG_Get()
while (tm_msg.msg_id)
{
	if (tm_msg.msg_id == 666)
	{
		i_flag_paf = vrai
		
		o_paf_sender = tm_msg.msg_sender
		v_paf_dir = tm_msg.msg_vec1
	}

	tm_msg = MSG_Get()
}