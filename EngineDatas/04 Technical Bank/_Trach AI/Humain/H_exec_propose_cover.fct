#include "H_defines.var"

messageid		tmid_LNK
object			to_serveur
float				tf_dist
object			to_main
vector			tv_temp

tmid_LNK = pop
if(joueur) return

to_serveur = MSG_GlobalGetGao(tmid_LNK, SERVEUR)
if (i_etat_courant == ETAT_IA_psss)
	tf_dist = 0.0
else
{
	tv_temp = LNK_InteractionPositionGet( tmid_LNK ) - OBJ_PosGet()
	tf_dist = MATH_VecNorm(tv_temp)
	if(ao_SRV[Ci_LNK_INTERACTION]) tf_dist += 1000000
}

LNK_ClientPropose(tmid_LNK, tf_dist)	 	
