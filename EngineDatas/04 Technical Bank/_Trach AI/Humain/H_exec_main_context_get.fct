messageid		EVT_ID

EVT_ID = EVENT_FindEventPereTarget(C_EVENT_TYPE_Visibility, o_main_actor, nobody)
if ( MSG_GlobalIsValid(EVT_ID))
{
	i_followed_context = EVENT_VisionContextGet(EVT_ID)
}
else
{
	// PAS de VISION du main !!
	DBG_BreakPoint()
}	
