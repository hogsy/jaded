#include "H_defines.var"

AI_Execute("H_exec_ch_Stimulus_Paf")
if (o_paf_sender)
{
	macro_change_etat(ETAT_commun_paf)
}

AI_Execute("H_exec_read_joy")

AI_Execute("H_exec_read_world")

if (i_wall_ID == 6 && i_flag_crouch)
{
	macro_change_etat(ETAT_joueur_plaque)
}
	
if (i_flag_tir)
{
	macro_change_etat(ETAT_commun_tir)
}
	
if (i_flag_frappe)
{
	macro_change_etat(ETAT_commun_frappe)
}