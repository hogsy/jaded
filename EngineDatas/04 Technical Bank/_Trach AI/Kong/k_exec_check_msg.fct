
message		tm_message
object			to_sender

i_flag_pose_ANN_this_trame = faux
MSG_SetNull(msg_last_message)

tm_message = MSG_Get()
while (tm_message.msg_id)
{
	if (tm_message.msg_id == msg_id_WP_Pose_ANN)
	{
		// Il y a un WP de type "Pose Ann" à proximité
		i_flag_pose_ANN_this_trame = vrai
	}

	msg_last_message = tm_message		// backup dernier message valide

	tm_message = MSG_Get()
}

