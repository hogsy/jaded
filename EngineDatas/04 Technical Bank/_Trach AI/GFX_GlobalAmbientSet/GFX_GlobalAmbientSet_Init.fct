#include "GFX_GlobalAmbientSet_Defines.var"
M_DeclareInteraction_Common

int ti_i
int i_key_for_this_level
i_key_for_this_level = WOR_GetKey()

for (ti_i = 0; ti_i < @"univ" WORLD_Key_Map_Nbr; ti_i++)
{
	if (@"univ" WORLD_Key_Map_Key[ti_i] == i_key_for_this_level )
	{
		DBG_TraceString( @"univ" WORLD_Key_Map_Name[ti_i] )
		break
	}
}

DBG_TraceString("Key for this level to enter in GFX ambiant AI: ")
DBG_TraceInt(i_key_for_this_level)
DBG_TraceEOL()

AI_TrackCurStop()