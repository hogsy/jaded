
int		ti_int
int		ti_fini

ti_fini = vrai
for ( ti_int = 0; ti_int < 5; ti_int++)
{
	if ( !ai_KC[ti_int])
		ti_fini = faux
	else
	{
		if ( MATH_RandFloat( 0.0, 1.0) < 0.5)
			@ao_morceaux[ti_int] OBJ_RotateLocalX(af_KC_X[ti_int] * TIME_GetDt())
		else
			@ao_morceaux[ti_int] OBJ_RotateLocalY(af_KC_Y[ti_int] * TIME_GetDt())
	}
}

if ( f_paf_time > 8.0)
{
	AI_TrackStop(3)	
}
f_paf_time += TIME_GetDt()