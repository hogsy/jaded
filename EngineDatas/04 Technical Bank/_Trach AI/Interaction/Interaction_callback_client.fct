int			TYPE

TYPE = AI_CBGetParam()

switch(TYPE)
{
	case Ci_LNK_INTERACTION :
		o_SRV = LNK_ServeurGet(Ci_LNK_INTERACTION, mid_SRV_LIAISON_ID, vrai, "Interaction_proposition", nofunc)
		break
		
	default:
}