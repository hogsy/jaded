///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#include "SND_BP_Constant.var"



///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// Define at least one function in a track
function track0 = "SND_BP_Init"
// function track1 = "name"
// function track2 = "name"
// function track3 = "name"
// function track4 = "name"



///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
private int	mai_State[SND_BP_Cte_SndRef_MaxNb]


///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
separator "Mode"
int 		i_InitState = enum("Stopped"0 "Playing"1)											// play at the beginning of the map ?
int 		i_TriggerScan = enum("Always active"10 "Inactive after 1st playing"11)		// play only once by map ?
int 		i_Play = enum("While trigger is on"20 "When trigger is on"21 )				// play while the trigger is on ?
int 		i_Stop = enum("None"30 "When trigger is off"31 )									// stop when the trigger is turning off ?
//
separator "Bank indexes"
int 		ai_Indexes[SND_BP_Cte_SndRef_MaxNb] = (-1, -1, -1, -1, -1)
//
separator "Triggers"
optim trigger 	at_Trigger[SND_BP_Cte_SndRef_MaxNb]
//
separator "Callbacks (trigger_exec pour les polios)"
trigger 	at_Callback[SND_BP_Cte_SndRef_MaxNb]		// excuted when playing the sound
trigger 	at_StopCallback[SND_BP_Cte_SndRef_MaxNb]		// excuted when stopping the sound







