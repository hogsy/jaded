#include "PLC_Defines.var"
vector v
float f
// get dt
dt=TIME_GetDt()
// use speed as a sight, if speed is high enough
if(!MATH_VecNullEpsilon(Speed))
{
	OBJ_SightGeneralSet(MATH_VecBlendRotate(OBJ_SightGet(),Speed,dt),OBJ_BankingGet())
}
// rotate on the visual pivot
PLC_Rotate(Pivot,RotVector,RotSpeed)
// gravity
Speed.z-=dt*C_PLC_Gravity
