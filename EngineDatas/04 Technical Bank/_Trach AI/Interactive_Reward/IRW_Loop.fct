Include_UltraProcedure_Header

if( AI_TriggerIsValid(Trig_Declenchement) && call_trigger(Trig_Declenchement) )
{
	if( ! AI_TriggerIsValid(Trig_Declenchement1) || call_trigger(Trig_Declenchement1) )
	{
		@get_global Proc_Global_Reward_Add(OBJ_Me())
	}
}

if( OBJ_CapaTest(CAPA_Reward_Activated) )
{
	AI_TrackCurChangeNow("IRW_Done")
}

