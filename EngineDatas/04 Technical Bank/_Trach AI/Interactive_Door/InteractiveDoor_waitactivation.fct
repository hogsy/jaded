int 		i
object 	o_levier

if (call_trigger( mt_Activate ) )
{
	for (i = 0; i < mi_PD_NbPilar; i++)
	{
		o_levier = mao_PD_PilarLevier[ i ]
		if (o_levier)
			@o_levier OBJ_CapaSet( 0,OBJ_Capa_15 )
	}
	AI_TrackCurChangeNow( "InteractiveDoor_loop" )
}
