#include "PNJ_Raptor_defines.var"

message 		tmsg_m

tmsg_m = MSG_Get()

switch(i_way_computation_mode)
{
	case Ci_WAY_MODE_VALA :
	case Ci_WAY_MODE_REQUIN :	
		return
}

i_external_wp_done_nb = 2
o_external_wp = tmsg_m.msg_gao1

