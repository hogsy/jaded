// DEMANDE D'ACCESSIBILITE CONCERNANT UN HUMAIN
if( KBC_Pos_in_Territory(@get_global v_flag_pos_accessible, faux) )	// pas de test de feu
	@get_global i_flag_pos_accessible = vrai
else
	@get_global i_flag_pos_accessible = faux

