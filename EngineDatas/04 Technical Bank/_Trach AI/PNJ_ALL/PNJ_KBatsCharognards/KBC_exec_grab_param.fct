messageid  tmid
object		to_bone

tmid = pop

if( o_Jack )
	v_grab_object_pos = @o_Jack OBJ_PosGet()			// ANN ne peut pas refuser !!!!
else
	v_grab_object_pos = OBJ_PosGet()

LNK_GrabServeurVisionIDSet(tmid, C_ID_BatCharognard)
LNK_GrabStatusSet(tmid, 0)
LNK_GrabPositionSet(tmid, v_grab_object_pos)
to_bone = ANI_CanalObjectGet(Anim_Canal_PiedDroit)
LNK_GrabBoneSet(tmid, to_bone)
