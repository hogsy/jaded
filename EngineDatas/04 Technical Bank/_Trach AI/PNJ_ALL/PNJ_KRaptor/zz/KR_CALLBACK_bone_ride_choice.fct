messageid		tmid_proposition
object			to_client
object			to_proposedBone
float				tf_dist

tmid_proposition = pop

to_client = MSG_GlobalGetGao(tmid_proposition, CLIENT)

// for now if it's not the <ventre> forghet about it
to_proposedBone = LNK_GrabBoneGet(tmid_proposition)

if (to_proposedBone != @o_KONG ANI_CanalObjectGet(Anim_Canal_Ventre))
{
	MSG_GlobalDelete(tmid_proposition, C_EVENT_DEL)
}


