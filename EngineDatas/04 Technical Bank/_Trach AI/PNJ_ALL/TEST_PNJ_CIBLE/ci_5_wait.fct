#include "ci_defines.var"

i_cur_mode = Ci_mode_wait_av_disparait
if (i_cur_mode != i_old_mode)
{
	// INIT
	i_old_mode = i_cur_mode
	f_time_ref = TIME_Get()
}

AI_Execute("ci_exec_test_attaque_suivre")

if (TIME_Elapsed(f_time_ref, f_delai_disparition))
{
	AI_TrackCurChangeNow("ci_6_desappear")
}