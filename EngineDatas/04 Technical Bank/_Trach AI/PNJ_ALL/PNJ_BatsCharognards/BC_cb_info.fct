// DEMANDE D'ACCESSIBILITE CONCERNANT UN HUMAIN = pas de test de feu
if( BC_Pos_in_Territory(@get_global v_flag_pos_accessible, faux) )
	@get_global i_flag_pos_accessible = vrai
else
	@get_global i_flag_pos_accessible = faux

