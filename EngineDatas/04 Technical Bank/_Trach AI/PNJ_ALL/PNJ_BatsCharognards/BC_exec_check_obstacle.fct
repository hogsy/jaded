#include 		"BC_defines.var"

i_flag_obstacle = faux

if( i_flag_cine )
	return

// TEST COLL WALL
BC_Obstacle_Check(COL_C_Wall)

// TEST COLL GROUND
if( o_cadavre )
	BC_Obstacle_Check(COL_C_Ground)

f_check_obstacle_dist = Cf_dist_obstacle_vol			// réinit dist check obstacles
