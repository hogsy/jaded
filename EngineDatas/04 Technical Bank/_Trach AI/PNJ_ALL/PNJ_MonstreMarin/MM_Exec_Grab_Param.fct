
messageid  tmid

tmid = pop

LNK_GrabServeurVisionIDSet(tmid, C_ID_MonstreMarin)
LNK_GrabStatusSet(tmid, 0)
v_grabbed_obj_pos = @o_machoire OBJ_PosGet()
//v_grabbed_obj_pos += @o_machoire MATH_VecLocalToGlobal(cvector(0.0, 2.0, 2.0))
//DBG_RenderVector(@o_machoire OBJ_PosGet(), v_grabbed_obj_pos - @o_machoire OBJ_PosGet(), color_rouge)
LNK_GrabPositionSet(tmid, v_grabbed_obj_pos)

