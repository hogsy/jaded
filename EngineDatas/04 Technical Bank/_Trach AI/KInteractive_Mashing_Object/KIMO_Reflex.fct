#include "KIMO_defines.var"

// Test coupure de liaison
i_flag_liaison_coupee_cette_trame = faux

//ADDED SURESH
f_global_time = TIME_Get()

// ZDM PIED
if( COL_ColSetActivationGet() & C_bit_zdm_pied )
{
	if( ACT_ActionGet() == i_kit_Anim_KK_Gagne || ACT_ActionGet() == i_kit_Anim_KK_Gagne_fixe )
		COL_ZonePosSet(C_zdm_pied, ZDM_Pos2)
	else
		COL_ZonePosSet(C_zdm_pied, ZDM_Pos)
}

//if( IO_KeyPressed(65))
//{
//	KIMO_SFXGlass(-1.0)
//	KIMO_SFXGlass(1.0)
//}
