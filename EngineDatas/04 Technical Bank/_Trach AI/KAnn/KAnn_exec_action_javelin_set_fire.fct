#include "KAnn_Defines.var"

if (ACT_ActionGet() != Ann_frappe_bambou)
{
	ACT_ActionSet(Ann_frappe_bambou)
	i_javelin_set_fire = faux
}
else
{
	if( ! i_javelin_set_fire && ANI_CurrentFrameGet(0) > 65 )
	{
		i_javelin_set_fire = vrai
		@get_Arme_Lance_path o_projectile mi_flamme = 1
	}
	if( ACT_ActionFinished() )
		i_action_completed = 1
}
