#include "Lance_defs.var"

vector	tv_axe
vector	tv_pos

Lance_SetOnFire(faux)

tv_axe = OBJ_SightGet() / 4
tv_pos = OBJ_PosGet() + (5.0 * tv_axe)

if( ! i_hide )
{
	LIBGFX_PoisonGaz(i_GFX_poison, tv_pos, tv_axe, mi_poison)
}

EVENT_AddEventPoison(mi_poison, OBJ_Me(), tv_pos, tv_axe)
